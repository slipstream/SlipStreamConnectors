<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<artifactId>SlipStreamConnector-Occi-python-rpm</artifactId>
	<name>SlipStream Connector for Occi - Python (RPM)</name>
	<packaging>pom</packaging>

	<parent>
		<groupId>com.sixsq.slipstream</groupId>
		<artifactId>SlipStreamConnector-Occi-python</artifactId>
		<version>3.14</version>
	</parent>

	<build>

		<plugins>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>

				<executions>

        <!-- NB! Repackaging of the tarball with tar CLI is a hack to to fix 
             an issue with tarball packing by the maven assembly plugin. The 
             tarball generated by the maven assembly plugin can't be properly 
             extracted by Python 2.7 'tarfile' module. 
        -->

					<execution>
						<id>unpack-conector</id>
						<goals>
							<goal>unpack</goal>
						</goals>
						<configuration>
							<artifactItems>
								<artifactItem>
								  <groupId>com.sixsq.slipstream</groupId>
								  <artifactId>SlipStreamConnector-Occi-python-bundle</artifactId>
									<version>${project.version}</version>
									<classifier>bundle</classifier>
									<type>tar.gz</type>
								</artifactItem>
							</artifactItems>
							<stripVersion>true</stripVersion>
						</configuration>
					</execution>

				</executions>

			</plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>regenerate-tarball</id>
            <phase>package</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>tar</executable>
              <arguments>
                <argument>-zcf</argument>
                <argument>${project.build.directory}/occiclient.tgz</argument>
                <argument>-C</argument>
                <argument>${project.build.directory}/dependency</argument>
                <argument>slipstream_occi</argument>
              </arguments>
            </configuration>
          </execution>

          <execution>
            <id>pyc</id>
            <goals>
              <goal>exec</goal>
            </goals>
            <phase>compile</phase>
            <configuration>
              <executable>python</executable>
              <arguments>
                <argument>-m</argument>
                <argument>compileall</argument>
                <argument>${project.build.directory}/dependency/slipstream_occi</argument>
              </arguments>
            </configuration>
          </execution>

          <execution>
            <id>pyo</id>
            <goals>
              <goal>exec</goal>
            </goals>
            <phase>compile</phase>
            <configuration>
              <executable>python</executable>
              <arguments>
                <argument>-O</argument>
                <argument>-m</argument>
                <argument>compileall</argument>
                <argument>${project.build.directory}/dependency/slipstream_occi</argument>
              </arguments>
            </configuration>
          </execution>

        </executions>
      </plugin>

			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>rpm-maven-plugin</artifactId>
				<extensions>true</extensions>
				<configuration>
					<classifier>${package.os}</classifier>
					<release>${BUILD_NUMBER}.${package.os}</release>
					<summary>SlipStream Connector for Occi - Python
%include %{_rpmconfigdir}/macros.python
</summary>
					<name>slipstream-connector-occi-python${slipstream.edition}</name>
					<group>Applications/Engineering</group>
					<vendor>SixSq</vendor>
					<packager>SixSq</packager>
					<license>Apache 2.0</license>
					<url>http://sixsq.com</url>
					<needarch>noarch</needarch>
					<description>
						SlipStream Connector for Occi - Python.
					</description>

					<obsoletes>
                                          <obsolete>slipstream-connector-occi-python</obsolete>
                                        </obsoletes>

					<requires>
					  <require>python &gt;= 2.6</require>
					  <require>slipstream-client${slipstream.edition}</require>
					  <require>occi-cli</require> <!-- http://repository.egi.eu/community/software/rocci.cli/4.3.x/releases/sl/6/x86_64/RPMS/ -->
					  <require>lcg-CA</require> <!-- http://repository.egi.eu/sw/production/umd/ -->
					</requires>

					<mappings>

						<mapping>
							<directory>/opt/slipstream/downloads</directory>
							<filemode>644</filemode>
							<username>root</username>
							<groupname>root</groupname>
							<directoryIncluded>false</directoryIncluded>
							<sources>
								<source>
									<location>${project.build.directory}/occiclient.tgz</location>
								</source>
							</sources>
						</mapping>

                        <mapping>
                          <directory>${slipstream.base.dir}/connectors/bin</directory>
                          <filemode>775</filemode>
                          <username>slipstream</username>
                          <groupname>slipstream</groupname>
                          <directoryIncluded>false</directoryIncluded>
                          <sources>
                            <source>
                              <location>../tar/bin</location>
                              <includes>
                                <include>occi-*</include>
                              </includes>
                            </source>
                          </sources>
                        </mapping>

                        <mapping>
                            <directory>/usr/bin</directory>
                            <sources>
                                <softlinkSource>
                                    <location>${slipstream.base.dir}/connectors/bin/occi-describe-instances</location>
                                </softlinkSource>
                                <softlinkSource>
                                    <location>${slipstream.base.dir}/connectors/bin/occi-run-instances</location>
                                </softlinkSource>
                                <softlinkSource>
                                    <location>${slipstream.base.dir}/connectors/bin/occi-terminate-instances</location>
                                </softlinkSource>
                            </sources>
                        </mapping>
            
                        <mapping>
                          <directory>%{py_sitedir}</directory>
                          <filemode>664</filemode>
                          <username>slipstream</username>
                          <groupname>slipstream</groupname>
                          <directoryIncluded>false</directoryIncluded>
                          <sources>
                            <source>
                              <location>target/dependency</location>
                              <includes>
                                <include>slipstream_occi/**/*</include>
                              </includes>
                            </source>
                          </sources>
                        </mapping>

					</mappings>

      <installScriptlet>
        <script>
mkdir -p $RPM_BUILD_ROOT/%{py_sitedir}
mv $RPM_BUILD_ROOT/%%py_sitedir/* $RPM_BUILD_ROOT/%{py_sitedir}
        </script>
      </installScriptlet>

				</configuration>
				<executions>
					<execution>
						<id>attach-rpm</id>
						<phase>package</phase>
						<goals>
							<goal>attached-rpm</goal>
						</goals>
					</execution>
				</executions>

			</plugin>
		</plugins>

	</build>

</project>
